# Python dependencies for the analysis engines, graph regenerator, and EDGAR puller.
#   pip install -r requirements.txt
openpyxl>=3.1
edgartools>=4.0     # deterministic SEC/EDGAR fetch (_tools/fetch_edgar.py)
